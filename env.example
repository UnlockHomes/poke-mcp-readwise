# Readwise API Token
# Get your token from: https://readwise.io/access_token
# Required for the MCP server to access Readwise API
READWISE_TOKEN=your_readwise_token_here

# MCP API Key (Optional but recommended for production)
# Used to authenticate requests to the MCP server
# Generate a secure random string (e.g., using: openssl rand -hex 32)
# If not set, the server will run without API key authentication (not recommended for production)
MCP_API_KEY=your_mcp_api_key_here